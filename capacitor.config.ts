import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.atvrush.game',
  appName: 'ATV Rush',
  webDir: 'www',
  server: {
    androidScheme: "http"
  },
};

export default config;
